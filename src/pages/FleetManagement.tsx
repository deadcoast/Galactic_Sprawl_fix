import * as React from 'react';
import { FormationTacticsPage } from '../components/combat/formations/FormationTacticsPage';

const FleetManagement: React.FC = () => {
  return <FormationTacticsPage />;
};

export default FleetManagement;
